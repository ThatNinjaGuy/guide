export interface Category {
  id: string;
  name: string;
  description?: string;
}

export interface Experience {
  id: string;
  name: string;
  description?: string;
}

export interface University {
  id: string;
  name: string;
  country?: string;
}
